import('dotenv/config').then(() => import('@supabase/supabase-js')).then(async ({createClient}) => { const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); const t = [{ fecha: '2026-04-11', cliente_op: 'OP(s): 5012', material_id: 25, cantidad_kg: 21000, semana: 2, mes: 4, anio: 2026, observaciones: 'Liquidación automática Formulación' }]; const { data, error } = await supabase.from('inventario_traslados').insert(t).select(); console.log('Err:', error); console.log('Data:', data); })
